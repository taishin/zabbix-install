reindex table acknowledges;
reindex table actions;
reindex table alerts;
reindex table applications;
reindex table auditlog;
reindex table auditlog_details;
reindex table autoreg_host;
reindex table conditions;
reindex table config;
reindex table dchecks;
reindex table dhosts;
reindex table drules;
reindex table dservices;
reindex table escalations;
reindex table events;
reindex table expressions;
reindex table functions;
reindex table globalmacro;
reindex table globalvars;
reindex table graph_discovery;
reindex table graph_theme;
reindex table graphs;
reindex table graphs_items;
reindex table groups;
reindex table help_items;
reindex table history;
reindex table history_log;
reindex table history_str;
reindex table history_str_sync;
reindex table history_sync;
reindex table history_text;
reindex table history_uint;
reindex table history_uint_sync;
reindex table host_inventory;
reindex table hostmacro;
reindex table hosts;
reindex table hosts_groups;
reindex table hosts_templates;
reindex table housekeeper;
reindex table httpstep;
reindex table httpstepitem;
reindex table httptest;
reindex table httptestitem;
reindex table icon_map;
reindex table icon_mapping;
reindex table ids;
reindex table images;
reindex table interface;
reindex table item_discovery;
reindex table items;
reindex table items_applications;
reindex table maintenances;
reindex table maintenances_groups;
reindex table maintenances_hosts;
reindex table maintenances_windows;
reindex table mappings;
reindex table media;
reindex table media_type;
reindex table node_cksum;
reindex table nodes;
reindex table opcommand;
reindex table opcommand_grp;
reindex table opcommand_hst;
reindex table opconditions;
reindex table operations;
reindex table opgroup;
reindex table opmessage;
reindex table opmessage_grp;
reindex table opmessage_usr;
reindex table optemplate;
reindex table profiles;
reindex table proxy_autoreg_host;
reindex table proxy_dhistory;
reindex table proxy_history;
reindex table opmessage_grp;
reindex table opmessage_usr;
reindex table optemplate;
reindex table profiles;
reindex table proxy_autoreg_host;
reindex table proxy_dhistory;
reindex table proxy_history;
reindex table regexps;
reindex table rights;
reindex table screens;
reindex table screens_items;
reindex table scripts;
reindex table service_alarms;
reindex table services;
reindex table services_links;
reindex table services_times;
reindex table sessions;
reindex table slides;
reindex table slideshows;
reindex table sysmap_element_url;
reindex table sysmap_url;
reindex table sysmaps;
reindex table sysmaps_elements;
reindex table sysmaps_link_triggers;
reindex table sysmaps_links;
reindex table timeperiods;
reindex table trends;
reindex table trends_uint;
reindex table trigger_depends;
reindex table trigger_discovery;
reindex table triggers;
reindex table user_history;
reindex table users;
reindex table users_groups;
reindex table usrgrp;
reindex table valuemaps;
